apiVersion: v1
kind: Secret
metadata:
  name: a-secret-{{ git_version | regex_replace('[^A-Za-z0-9-]', '-') }}
  namespace: default
  labels:
    prefix: a_secret
type: Opaque
data:
  a_secret: "{{ 'not_very_secret_a' | b64encode }}"
  AN_ENV_VAR: "{{ 'not so secret either' | b64encode }}"
